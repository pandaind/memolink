Use the list_memories tool to retrieve every memory ID in the vault.
Then group the IDs by apparent topic and present a concise summary so the user can decide what to read next.
{{#hasMetadataFields}}
Memories in this vault may include the following frontmatter metadata fields:
{{#metadataFields}}
- **{{name}}**: {{description}}{{#required}} *(required)*{{/required}}
{{/metadataFields}}
{{/hasMetadataFields}}
